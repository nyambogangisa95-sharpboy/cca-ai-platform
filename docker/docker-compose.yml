version: "3.9"
services:
  cca-backend:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    environment:
      - PORT=4000
      - NODE_ENV=production
    ports:
      - "4000:4000"
    restart: unless-stopped
